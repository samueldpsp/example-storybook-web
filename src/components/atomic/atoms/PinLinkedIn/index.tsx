import React from 'react';
import { TiSocialLinkedin } from 'react-icons/ti';

import { Pin } from '../Pin';

export const PinLinkedIn: React.FC = () => {
  return (
    <Pin>
      <TiSocialLinkedin size={24} />
    </Pin>
  )
}
