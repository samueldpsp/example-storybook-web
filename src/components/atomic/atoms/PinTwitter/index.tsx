import React from 'react';
import { TiSocialTwitter } from 'react-icons/ti';

import { Pin } from '../Pin';

export const PinTwitter: React.FC = () => {
  return (
    <Pin>
      <TiSocialTwitter size={24} />
    </Pin>
  )
}
